# User-level nix config. Redundant once nix-darwin is active (it sets
# experimental-features system-wide), but required to bootstrap a fresh
# machine: without it, plain nix can't run `nix build` on this flake.
experimental-features = nix-command flakes
